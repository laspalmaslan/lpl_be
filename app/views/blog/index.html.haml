- @posts.each do |post|
  %article
    %header
      .title.pull-left
        %strong
          = link_to post.title, article_path(post)
      .social.pull-right
    %section
      = post.intro.html_safe
    %footer
      .read_more
        = link_to 'Leer más >>', article_path(post)
