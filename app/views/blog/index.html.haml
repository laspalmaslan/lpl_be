- @posts.each do |post|
  %article
    %header
      .title.pull-left
        %strong
          = link_to post.title, article_path(post)
      .social.pull-right
        = content_tag :div, "",class: "twitter", data: {text: post.title}
        = content_tag :div, "",class: "facebook", data: {text: post.title}
    %section
      = post.intro.html_safe
    %footer
      .read_more
        = link_to 'Leer más >>', article_path(post)
