%table.table.table-bordered.table-striped.table-hover
  %tr
    %th Nombre
    %th Nickname
    %th Edad
    %th Email
    %th Tipo
    %th Clan
    %th Pagó el
    %th Acciones

  - @enrollments.each do |enrollment|
    %tr
      %td= enrollment.full_name
      %td= enrollment.nick
      %td= enrollment.age
      %td= enrollment.email
      %td= enrollment.etype.name
      %td= enrollment.clan_id
      %td= enrollment.paid_at
      %td
        .btn-group
          = link_to enrollment_unpaid_path(enrollment.id), method: :delete, data: { confirm: '¿Estás seguro de querer cancelar este pago?' }, class: "btn btn-warning with-tooltip" , :title => "Cancelar pago" do
            %i.success - €
