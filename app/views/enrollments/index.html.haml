%table.table.table-bordered.table-striped.table-hover
  %tr
    %th Nombre
    %th Dni
    %th Edad
    %th Acciones

  - @enrollments.each do |enrollment|
    %tr
      %td= enrollment.full_name
      %td= enrollment.dni
      %td= enrollment.age
      %td
        .btn-group
          = link_to edit_enrollment_path(enrollment), class: "btn btn-primary with-tooltip" , :title => "Editar" do
            %i.icon-pencil
          = link_to enrollment, method: :delete, data: { confirm: '¿Estas seguro de eliminar este post?' }, class: "btn btn-danger with-tooltip" , :title => "Borrar" do
            %i.icon-trash
